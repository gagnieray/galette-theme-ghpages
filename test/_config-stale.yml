# CI only, overlaid on _config.yml to build the demo with a fixture whose rank has
# been dropped just below the current Galette's — the state a plugin lands in when
# Galette releases a new generation and the plugin has not followed. The fixture is
# generated by the CI step, from the committed one, so no version is written here.
data_dir: test/data-stale
